// const initialState = {
//   stocks: []
// };

// const Stocks = (state = initialState, action) => {
//   switch (action.type) {
//     case "GET-STOCK":
//       return { ...state, stocks: action.payload };}

//       switch (action.type) {
//         case "UPDATESTOCK":
//          return {...state,stocks:state.stocks.map((el,id)=>
//             id===action.payload.id ?
//             {...el, title:action.payload.task }: el)}

//     default:
//       return state;
//   }
// };
// export default Stocks;
